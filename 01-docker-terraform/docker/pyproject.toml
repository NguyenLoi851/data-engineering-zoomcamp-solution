[project]
name = "data-ingestion"
version = "0.1.0"
description = "Data ingestion script for PostgreSQL"
requires-python = ">=3.9"
dependencies = [
    "pandas>=2.0.0",
    "sqlalchemy>=2.0.0",
    "psycopg2-binary>=2.9.0",
    "pyarrow>=14.0.0",
]
